export const metadata = {
  title: 'Dilution Scenarios | IPOReady',
  description: 'Analyze share ownership changes from warrant exercises, financing, and vesting',
}

export default function DilutionScenariosLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
